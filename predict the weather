#include <stdio.h>

int main(void) {
    int n;
    scanf("%d",&n);
    char str[1000];
    scanf("%s",str);
   // for(int i=0;str[i] != '\0';i++) {
        if(str[n-1]=='o')
        printf("yes");
        else
        printf("no");
    
	// your code goes here
	return 0;
}

